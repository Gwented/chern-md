// Work on illegal tokens and correctly unwinding while lexing.
// Typedef will merge with corresponding type if needed during semantic analysis
// Or maybe just a flat table of type defs in a new type that IS the struct/enum for ease of use?
@def
    var->
        name: str(Len(~8), !IsEmpty)
        floor: Set<u32>
    /*nest->
        .person:= {
            names: List<str>
            floor: Set<u32>
        }*/
    complex_rules->
@end

// I feel like expecting (file_size / 30) new lines is a good with_capacity metric if storing them \n
// If file_size > 300 bytes I will CAP it out at 300 since that likely means we are in a serialized file.
// For in serialized file definitions, should be (file_size / 40 + 4)

// IF this file is found it will override

person:= {
    name: "Floor"
    age: 20
    car:= {
        name: "Car"
        age: 6
    }
}
